[project]
name = "uv-tool-constraints"
version = "0.1.0"
dependencies = ["requests"]

[project.scripts]
uv-tool-constraints = "acme.main:main"

[tool.uv]
constraint-dependencies = ["urllib3<2", "requests<3"]
override-dependencies = ["idna==3.7"]
