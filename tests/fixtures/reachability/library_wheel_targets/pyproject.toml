[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "library-wheel-targets"
version = "0.0.0"

[tool.hatch.build.targets.wheel]
packages = ["src/acme"]

[tool.chokkin]
mode = "library"
