[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "build-maturin"
version = "0.1.0"
dependencies = ["numpy"]

[tool.maturin]
python-source = "python"
module-name = "acme._core"
