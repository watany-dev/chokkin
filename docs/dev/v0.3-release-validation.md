# v0.3 Release Validation

Date: 2026-07-01
Rust: `rustc 1.93.0 (254b59607 2026-01-19)` (MSRV)

## Verdict

v0.3 release validation is passing for the measured gates below. v0.3 is the
**Contract Stabilization** release (Phase 3): it adds `schema_version: "1"` on
JSON reporter and baseline output, publishes JSON Schema under `docs/schema/`,
wires `[tool.chokkin.severity]` per-rule overrides through emit/reporters/exit
codes, stabilizes SARIF rule metadata, freezes ignore directive regression
tests, and documents the plugin API RFC. Baseline CI adoption continues via
this repository's dogfood job in `.github/workflows/ci.yml`.

| Criterion | Evidence | Result |
|---|---|---|
| Pre-release gate | `make check` (fmt / clippy / doc / test / deny / machete) | PASS |
| OSS gate continues passing | `make oss-clones`; `make oss-metrics ARGS=--gate` over 20 pinned projects plus 2 recall sentinels | PASS |
| 10k files warm cache <= 2s | `parse_cache_warm/src/10000` median `172.96 ms` in `make bench` | PASS |
| Baseline schema upgrade | `--update-baseline` wrote `schema_version: "1"` / `chokkin_version: "0.3.0"` / 182 issues | PASS |
| Baseline CI adoption | `--baseline chokkin-baseline.json --reporter github` suppressed 182 issues, exit 0; `--reporter sarif` emitted zero unsuppressed results, exit 0 | PASS |

## Commands Run

```bash
make check
scripts/run-oss-fixture.sh --build
make oss-clones
make oss-metrics ARGS=--gate
make bench
target/release/chokkin --baseline chokkin-baseline.json --update-baseline
target/release/chokkin --baseline chokkin-baseline.json --reporter github
target/release/chokkin --baseline chokkin-baseline.json --reporter sarif
```

## Gate Results

`make check` completed with:

- `cargo fmt -- --check`
- `cargo clippy --all-targets --locked -- -D warnings`
- `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --locked`
- `cargo test --locked` (270+ tests, 0 failed)
- `cargo deny check advisories licenses bans sources`
- `cargo machete`

`make oss-metrics ARGS=--gate` completed successfully after `make oss-clones`:

| Criterion | Target | Measured | Result |
|---|---|---|---|
| Unused-dep FP rate (CHK002) | < 5% | 0.0% (0 FP / 2 reported, 0 unclassified) | PASS |
| Unused-dep recall (CHK002 tp) | all detected | 2/2 detected | PASS |
| Crashes (exit 3) | 0 | 0 | PASS |
| Cold run, medium project | <= 2000 ms | all within budget (max 72 ms typer) | PASS |

## Warm Cache Measurements

`make bench`:

| Benchmark | Median (point estimate) |
|---|---:|
| `parse_cache_warm/src/100` | `1.6115 ms` |
| `parse_cache_warm/src/1000` | `16.022 ms` |
| `parse_cache_warm/src/5000` | `83.626 ms` |
| `parse_cache_warm/src/10000` | `172.96 ms` |

The 10k warm cache result is comfortably below the 2s target.

## Baseline CI Adoption

The dogfood baseline was regenerated for v0.3:

- baseline file: `chokkin-baseline.json`
- schema: `schema_version: "1"` (v0.3 stable contract)
- `chokkin_version`: `"0.3.0"`
- issues: 182 (all reachable from test fixtures)

Local verification on 2026-07-01:

| Command | Result |
|---|---|
| `target/release/chokkin --baseline chokkin-baseline.json --update-baseline` | wrote 182 issues with `schema_version: "1"` |
| `target/release/chokkin --baseline chokkin-baseline.json --reporter github` | suppressed 182 issues, exit 0 |
| `target/release/chokkin --baseline chokkin-baseline.json --reporter sarif` | emitted SARIF with CHK001–CHK010 rule metadata (`helpUri` / `fullDescription` / `defaultConfiguration.level`), zero unsuppressed results, exit 0 |

## v0.2 reader compatibility

The v0.3 baseline reader continues accepting v0.2 baseline files without
`schema_version` (omitted field defaults to the v0.2 draft). This was verified
before regeneration: the old `chokkin_version: "0.2.0"` baseline (no
`schema_version`) was loaded and suppressed 182 issues with exit 0.
