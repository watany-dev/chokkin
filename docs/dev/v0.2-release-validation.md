# v0.2 Release Validation

Date: 2026-06-15
Rust: `rustc 1.93.0 (254b59607 2026-01-19)`

## Verdict

v0.2 release validation is passing for the measured gates below. Baseline CI
adoption is covered by this repository's dogfood job in `.github/workflows/ci.yml`,
which runs chokkin against the checked-in `chokkin-baseline.json`.

| Criterion | Evidence | Result |
|---|---|---|
| 10k files warm cache <= 2s | `parse_cache_warm/src/10000` median `186.85 ms` in `make bench`; `204.21 ms` in `bench-save`; `189.86 ms` in `bench-cmp` | PASS |
| OSS gate continues passing | `make oss-clones`; `make oss-metrics ARGS=--gate` over the 20 pinned projects plus recall sentinels | PASS |
| Pre-release gate | `make check`; `scripts/run-oss-fixture.sh --build`; `make oss-metrics ARGS=--gate` | PASS |
| Baseline CI adoption case | `.github/workflows/ci.yml` job `chokkin-baseline` runs `cargo run --locked --release -- --baseline chokkin-baseline.json --reporter github`; `chokkin-baseline.json` is checked in with 154 current issues; local verification suppressed all 154 issues with exit 0 | PASS |

## Commands Run

```bash
make check
scripts/run-oss-fixture.sh --build
make oss-clones
make oss-metrics ARGS=--gate
make bench
make bench-save BASELINE=main
make bench-cmp BASELINE=main
target/release/chokkin --baseline chokkin-baseline.json --update-baseline
target/release/chokkin --baseline chokkin-baseline.json --reporter github
target/release/chokkin --baseline chokkin-baseline.json --reporter sarif
```

## Gate Results

`make check` completed with:

- `cargo fmt -- --check`
- `cargo clippy --all-targets --locked -- -D warnings`
- `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --locked`
- `cargo test --locked`
- `cargo deny check advisories licenses bans sources`
- `cargo machete`

`scripts/run-oss-fixture.sh --build` completed successfully:

- fixtures: `unused_boto3`, `src_layout`, `empty`
- summary: `target/oss-fixtures/summary.tsv`

`make oss-metrics ARGS=--gate` completed successfully after `make oss-clones`:

| Criterion | Target | Measured | Result |
|---|---|---|---|
| Unused-dep FP rate (CHK002) | < 5% | 0.0% (0 FP / 2 reported, 0 unclassified) | PASS |
| Unused-dep recall (CHK002 tp) | all detected | 2/2 detected | PASS |
| Crashes (exit 3) | 0 | 0 | PASS |
| Cold run, medium project | <= 2000 ms | all within budget | PASS |

## Warm Cache Measurements

`make bench`:

| Benchmark | Median |
|---|---:|
| `parse_cache_warm/src/100` | `1.8495 ms` |
| `parse_cache_warm/src/1000` | `18.920 ms` |
| `parse_cache_warm/src/5000` | `92.125 ms` |
| `parse_cache_warm/src/10000` | `186.85 ms` |

`make bench-save BASELINE=main`:

| Benchmark | Median |
|---|---:|
| `parse_cache_warm/src/10000` | `204.21 ms` |

`make bench-cmp BASELINE=main`:

| Benchmark | Median | Comparison |
|---|---:|---|
| `parse_cache_warm/src/10000` | `189.86 ms` | No change detected (`p = 0.20`) |

The 10k warm cache result is comfortably below the 2s target in all three benchmark runs.

## Baseline CI Adoption

This repository now dogfoods baseline CI adoption:

- baseline file: `chokkin-baseline.json`
- CI job: `.github/workflows/ci.yml` / `chokkin-baseline`
- primary PR check command: `cargo run --locked --release -- --baseline chokkin-baseline.json --reporter github`
- SARIF artifact command: `cargo run --locked --release -- --baseline chokkin-baseline.json --reporter sarif > chokkin.sarif`

Local verification on 2026-06-15:

| Command | Result |
|---|---|
| `target/release/chokkin --baseline chokkin-baseline.json --update-baseline` | wrote 154 issues |
| `target/release/chokkin --baseline chokkin-baseline.json --reporter github` | suppressed 154 issues, exit 0 |
| `target/release/chokkin --baseline chokkin-baseline.json --reporter sarif` | emitted SARIF with zero unsuppressed results, exit 0 |
